# Parsing Module

Responsibility: extract normalized text from ingested raw sources and preserve provenance such as file, page, row, and section metadata for downstream chunking and citation work.

Current status: supports PDF, TXT, Markdown, CSV-ticket, and JSON-ticket parsing with explicit parse failures and conservative text normalization.
