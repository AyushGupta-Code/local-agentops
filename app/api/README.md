# API Module

Responsibility: expose HTTP endpoints, wire request/response models to backend services, and keep transport-layer concerns separate from domain logic.

Current status: includes a minimal health and architecture endpoint for bootstrapping.
