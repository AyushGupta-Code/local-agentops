# Indexing Module

Responsibility: prepare local vector and lexical indexing manifests from persisted chunk records while preserving clean backend abstractions for future replacement.

Current status: includes local manifest preparation, lexical term extraction, and a swappable vector backend interface.
