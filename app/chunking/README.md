# Chunking Module

Responsibility: segment parsed documents into deterministic retrieval chunks while preserving provenance such as source file, page numbers, row ranges, and section titles.

Current status: supports plain-text sliding windows, section-aware document chunking, and support-ticket chunking with configurable overlap.
