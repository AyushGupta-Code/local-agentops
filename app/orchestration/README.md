# Orchestration Module

Responsibility: classify user requests into deterministic task types and return an inspectable orchestration plan showing which pipeline components should run.

Current status: rule-based query router and plan builder only. This module does not implement an autonomous agent loop.
