# Retrieval Module

Responsibility: run local-first vector and hybrid retrieval over manifest-backed chunk indexes, then return ranked evidence with provenance and citation metadata.

Current status: includes deterministic local embeddings, manifest-backed vector and lexical retrieval, and simple weighted reranking.
