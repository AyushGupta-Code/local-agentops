# Models Module

Responsibility: define shared typed schemas, DTOs, and domain records used across API, pipelines, and storage layers.

Current status: includes a minimal set of transport and pipeline models for scaffolding.
