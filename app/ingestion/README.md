# Ingestion Module

Responsibility: discover local source files, extract lightweight provenance metadata, and register raw document or support-ticket records without doing deep parsing yet.

Current status: supports PDF, TXT, Markdown, CSV, and JSON ingestion with per-file error reporting.
