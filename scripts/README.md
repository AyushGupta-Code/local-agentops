# Scripts Directory

Responsibility: store developer utilities for local runs, maintenance tasks, and one-off project automation.

Current status: includes `index_chunks.py` for preparing local vector and lexical index manifests from persisted chunk files.
